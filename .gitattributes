# ============================================================================
# .gitattributes —— 固化换行符与文件类型识别
#
# 为什么需要：
#   本仓库在 Windows 上开发（并可能被 Linux/macOS 贡献者 clone）。若不固化规则，
#   换行符随各人 core.autocrlf 设置漂移，会导致「整文件 diff」「PR 体积虚高」
#   以及纯 LF / 纯 CRLF 混用（本仓库初期实测：10 个抽样文件中 5 个 CRLF、5 个 LF）。
#
# 策略：
#   · 文本文件一律以 LF 入库、LF 检出 —— 保证任何平台 clone 下来内容一致；
#   · Windows 专属脚本（.bat/.cmd/.ps1）保持 CRLF，否则可能无法执行；
#   · 二进制文件禁止行尾转换与文本 diff。
# ============================================================================

# 默认：自动识别文本，统一 LF
* text=auto eol=lf

# --- Windows 专属脚本：必须 CRLF ---
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# --- 明确文本类型 ---
*.ts    text
*.tsx   text
*.js    text
*.cjs   text
*.mjs   text
*.json  text
*.css   text
*.html  text
*.md    text
*.rs    text
*.toml  text
*.yml   text
*.yaml  text
*.txt   text

# --- 二进制：禁止转换与 diff（否则 git 会尝试按文本处理并可能损坏文件）---
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.bmp   binary
*.ico   binary
*.icns  binary
*.exe   binary
*.dll   binary
*.lib   binary
*.so    binary
*.dylib binary
*.pdb   binary

# --- 锁文件：不参与 diff、不计入 GitHub 语言统计 ---
pnpm-lock.yaml linguist-generated=true -diff
Cargo.lock     linguist-generated=true -diff
