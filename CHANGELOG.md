# 更新日志

本文件记录 Mindscape / 脑海空间 的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **自定义快捷键** —— 设置面板新增「快捷键」页：按「视图 / 编辑 / 卡片 / 画布 / 布局」
  分组列出全部可绑定操作及其当前键位；点键位按钮即进入录制态，按下想要的组合键
  即可改绑（`Esc` 取消）；每个操作可单独「恢复默认」，也可一键「全部恢复默认」。
  键位匹配一律按物理键位判定，且会**检测冲突**——改绑到已被其它操作占用的组合时
  当场拒绝并提示是被谁占用的；旧版遗留写法（如 `Ctrl+Shift+0`）只在操作仍为默认
  绑定时生效，一旦改绑立刻让位。绑定关系自动持久化保存
- **可拖动 / 可缩放的设置弹窗** —— 设置面板由固定侧栏改为浮动弹窗：点设置按钮弹出，
  拖标题栏移动、拖右下角手柄缩放，位置与尺寸都会记住；缩小窗口时浮窗自动收回可视区，
  按 `Esc` 或点关闭按钮收起。弹窗内以左侧页签分页，当前含「快捷键」与「版本更新」
  两页，后续新增页面只需登记一项配置
- **顶栏快捷图标** —— 顶栏设置按钮左侧新增两个纯图标按钮：深 / 浅色切换（太阳 / 月亮
  图标直接切换，不显示文字）与「显示已移除」（其左侧，带移除计数徽标）
- **插件管理（设置面板新增「插件」页）** —— 一处管理全部插件：查看列表（内置 / 外部、
  版本、状态、已注册内容与数量）、**启用 / 停用**（停用即从界面撤下它提供的一切，
  已有卡片数据不受影响）、**卸载**（外部插件，删安装目录但**保留插件配置**）、
  **重新加载**（外部插件替换文件后生效）与**刷新**（重新扫描插件目录）。
  插件随应用一起发布的内置能力默认启用；用户放进插件目录的外部插件默认只登记不启用，
  看清后再手动开。插件状态存在 `%APPDATA%\Mindscape\plugins.json`（原子写，
  文件损坏时自动备份 `.bak` 并以空列表启动）
- **色卡插件（内置）** —— 画布空白处右键「新建色卡…」弹出插件自带的编辑面板：
  可选颜色（8 色预设 / 系统取色器 / 手输色值）、尺寸（**比例预设 1:1 / 16:9 / 9:16 ×
  长边像素预设 512 / 1024 / 1920 / 2560**，或常用尺寸预设、自定义宽高）、
  文件名前缀与**输出文件夹**（默认当前空间目录，可另选）；点「生成并保存」即按参数
  生成 **PNG 图片**并写入该文件夹（重名自动加序号）。色卡就是**一整块纯色**，
  图片内部不写任何文字或标签。输出文件夹在空间内时还会顺手在画布上放一张对应卡片；
  参数会被记住，下次打开沿用
- **色卡色号改为悬停在画布上显示** —— 色卡图片是**一整块纯色**，色号绝不画进图片
  （「在色卡图片上标注色值」这一选项已彻底移除）；把鼠标移到画布上的色卡时，
  色号才会显示在**图片区域外的左下角**（与右下角的分辨率标签左右对称）
- **图片卡悬停显示实际分辨率** —— 把鼠标移到画布上的任意图片卡时，卡片**下方外侧
  右对齐**处淡入一枚半透明的等宽字体标签，显示该图的实际分辨率「宽 × 高」（像素）；
  移出即隐。标签不占用卡片盒空间、不遮挡图片内容；图片尚未加载完成或加载失败时
  不显示数值（失败时显示「尺寸不可读」），拖入、粘贴与插件生成的图片自动同样生效
- **画布空白菜单支持插件扩展** —— 插件可注册自己的画布菜单项，插在「新建便签 / 粘贴」与
  「撤销 / 重做」之间；插件停用后该项立即消失
- **插件开发文档** —— 新增 `docs/插件开发指南.md`：从零写一个插件的完整路径
  （清单契约、API 全量参考、五个扩展点的实际可用状态、通用 meta 约定、
  持久化、门禁规则、调试与排错对照表、可复制示例）

### 变更

- **设置面板瘦身** —— 「切换深浅色模式」与「显示已移除」从面板内移出，改为顶栏图标
  按钮；外观行为不变，只是入口换了位置
- **取消功能折叠栏** —— 移除顶栏可折叠的功能按钮栏；其中的撤销 / 重做、复制 / 粘贴、
  删除、恢复卡片等功能统一并入**右键菜单**（菜单项右侧显示当前键位），画布与卡片
  的右键菜单都按场景裁剪可用项
- **为插件功能做准备：代码清理与去重**（内部改动，界面与行为不变）—— 删除 2 个随功能
  折叠栏下线的孤儿图标；把 4 处重复的 `zoom > 0 ? zoom : 1` 收成 `safeZoom`、2 份补零
  函数收成 `pad2`、2 份错误消息归一化收成 `toErrorMessage`（顺带修掉其中一份漏判裸字符串
  导致的提示漂移）；把 2 份逐行相同的矩形并集 / 等比适配算法收成 `core/geometry/rect.ts`，
  并统一了 4 个各自声明的矩形类型；`spaces.json` 与布局文件的原子写收成
  `core/storage/atomicWrite.ts`；标签读写函数从命令层移到 `core/board/cardMeta.ts`，
  修掉「配置层反向依赖命令层」的层次倒置
- **架构守卫规则 4 由黑名单改为白名单** —— 原规则只禁止 core 依赖 5 个已知目录，任何新增
  的顶层目录都能绕过分层约束；现改为「core 只允许依赖 `@/core/**`、core 内的相对路径与
  外部包」，并改用支持跨行 import 的扫描方式（旧写法会整段漏掉多行 import）。同时把 6 处
  过时注释订正为与现状一致，过时的《缩略图重构计划》移入 `docs/archive/` 并加归档说明
- **为插件功能做准备：拆分超大文件 Board / Canvas**（内部改动，界面与行为不变）—— 这两个
  文件的行数此前已顶到守卫上限（Board 只剩 24 行余量、Canvas 已顶死），再往里加任何逻辑都会
  触发红线。本次把**画布右键菜单的组装**整块抽到 `pages/board/contextMenus.tsx`（纯函数，
  副作用全部以回调注入，配 19 条单测），把**画布快捷键的监听与派发**抽到
  `canvas/useCanvasShortcuts.ts`（派发拆成不碰 window 的纯函数，配 10 条单测）；
  Board 2094 → 1977 行、Canvas 1192 → 1154 行，并为后续插件接线留出余量
- **插件代码集中到独立文件夹**（内部改动，界面与行为不变）—— `src/plugins/` 改为
  「一个插件一个文件夹」：`colorCard/`（色卡）、`demo/`（示例插件，仅测试调用），
  每个文件夹自带文案、参数、生成逻辑与界面；根目录只留 `index.ts`（统一入口）、
  `builtinPlugins.ts`（内置清单）与 `bootstrap.ts`（启动引导）。插件**不直接引用宿主的
  注册函数**，只使用宿主注入的 `api`（否则宿主无从知道哪些注册属于哪个插件，
  也就无法实现停用 / 卸载）。另有 31 个新单测覆盖插件与色卡链路

### 修复

- **「适应内容」快捷键无法生效** —— 原组合键 `Ctrl+Shift+0` 按不出效果，两个原因
  叠加：① Shift 会把字符键变成上档字符（`Shift+0` 产生 `)`），旧实现按
  `key === '0'` 匹配永远落空 → 改为按物理键位（`Digit0` / 小键盘 `Numpad0`）判断；
  ② 实测该组合键在本机被输入法 / 系统占走——探针显示应用只收到 `Control` / `Shift`，
  数字 `0` 的 `keydown` 根本没送达，代码层无法绕过。主组合键因此改为
  **`Ctrl+Alt+0`**（该组合无人占用），`Ctrl+Shift+0` 继续兼容；`Ctrl+0` 复原视图
  改用同一套物理键位判断
- **便签编辑时占位文字与输入内容重叠** —— 新建便签进入编辑后，底层的
  「（空便签）」占位文字要等保存才消失，透过透明的输入框与正在输入的
  文字叠在一起；现在进入编辑态即不再渲染正文与占位文字，输入内容清晰可见

## [0.5.0] - 2026-09-13

### 新增

- **跨应用复制粘贴** —— 在空间里复制文件卡后，可直接在资源管理器、桌面或任何
  支持文件粘贴的软件里 `Ctrl+V` 粘贴出文件副本；反过来，从外部复制文件后在
  空间里 `Ctrl+V` 即可导入（与拖入同一套链路：复制进空间，原件不动）。
  纯便签复制到外部时粘贴的是便签文字。粘贴进空间的落盘规则与粘贴卡片一致：
  选中分区 → 该分区，否则空间主目录
- **便签四向缩放** —— 便签选中后，上 / 下 / 左 / 右四个边中点的手柄可以直接
  拖拽调整大小（原来只有右下角一处）；西 / 北边拖动时对边保持固定，
  拖到最小尺寸时位置同步停住。图片卡片仍只从右下角等比缩放（保持原图比例）

### 变更

- **图片卡备注 / 标签同行显示** —— 悬浮层内的备注条与标签条由纵向堆叠改为
  横排同一行（行内垂直居中），排版更紧凑；两者合计超宽时整条换行，不截断内容
- **空间卡操作按钮外挂卡片外** —— 收藏 / 重命名 / 导出布局 / 移除按钮改为
  悬停在卡片**外部右上角**（紧贴卡片上边缘、右对齐），悬停显示、移开隐藏，
  与卡片本体零重叠，标题任何情况下都不被遮挡
- **图片卡的备注 / 标签改为悬浮层** —— 叠加显示在图片左上方（半透明底 + 边框，
  压在图片上清晰可读），不再占用卡片底部空间：添加备注和标签不会改变卡片
  的显示比例、不再出现留白，悬浮层随卡片一同移动。文件卡与便签的显示不变
- **粘贴不复制备注与标签** —— 应用内复制粘贴文件 / 图片卡片时只搬运内容本身
  （图片即原图字节），原卡的备注与标签不会带到新卡片；其余信息的复制行为不变
- **空间列表长名称** —— 空间卡片的操作按钮（收藏 / 重命名 / 导出布局 / 移除）
  改为悬停浮层，不再挤压标题；长名称以省略号截断，悬停可看完整名称

### 修复

- **浅色模式下便签看不清** —— 便签改用专属「便签纸」底色（浅色淡黄 /
  深色暗琥珀灰）并加深边框、提升阴影，与画布背景明显区分；深浅主题均生效
- **编辑便签时可以正常选中文字** —— 在编辑状态下用鼠标划选、双击选词不再
  误触发拖动整个便签（此前画布的全局按下监听会抢先把它当成拖拽手势）；
  编辑、拖动、缩放三种操作互不干扰

## [0.4.0] - 2026-09-13

### 新增

- **画布搜索** —— `Ctrl+F` 唤出搜索浮层：按文件名、便签正文或分区名匹配卡片，
  `Enter` / `Shift+Enter` 在命中项之间循环跳转（自动选中并把卡片带到视口中心），
  命中卡片画虚线高亮、当前目标加粗
- **缩放到全部内容** —— `Ctrl+Shift+0`（或状态条按钮）把所有卡片与分区
  一键收进当前视口，误拖到远处后一键找回
- **空间改名 / 收藏 / 排序** —— 空间卡片上可直接重命名（只改显示名，文件夹不动）、
  切换收藏；列表按「收藏 → 最近打开」排序，老数据无需任何处理自动兼容
- **导出布局 / 导入空间** —— 空间卡片上的「导出布局」把当前摆放与连线写成
  `<目标文件夹>\mindscape-layout.json`；空间列表页右上角的「导入空间」选中带该文件的
  文件夹即可新建空间并还原布局（收编进软件目录后才问是否删除源文件，删不删由你决定）

### 变更

- **便签改为双击行内编辑** —— 双击便签直接在便签本体内改文字（光标定位末尾），
  不再弹出编辑对话框；点击便签外部或按 `Esc` 保存退出，卡片尺寸样式始终不变；
  右键菜单「备注」对便签同样进入行内编辑
- **布局改存软件目录** —— 画布布局从 `<空间文件夹>\.mindscape\layout.json` 搬到
  `%APPDATA%\Mindscape\layouts\<空间 id>.json`，空间文件夹里**不再出现任何软件生成的文件**；
  老版本留下的 `.mindscape\layout.json` 会在首次打开时自动迁移，**旧文件不会被删除**
- **未分组文件直接落空间主目录** —— 拖入 / 粘贴不再创建「未分类」子文件夹，
  未归入任何分区的文件直接放在空间主目录；「移动到…」菜单里同样可以把文件移到主目录
  （旧版「未分类」文件夹里的历史文件也可由此移出，旧文件夹本身不会被自动改动）

### 修复

- **便签与标签在退出空间重进后消失** —— 便签没有对应的磁盘文件，重进空间时
  被布局合并逻辑误判为「文件已丢失」而整卡丢弃，下一次落盘把残缺状态固化；
  现在无文件卡片（便签，以及将来插件注册的无文件卡片类型）一律按布局原样恢复，
  便签上的标签、备注、位置不再丢失。文件卡片上的标签本身不受此问题影响

## [0.3.0] - 2026-09-12

### 新增

- **小地图** —— 画布右下角提供整体概览，实时绘制分区、卡片与当前视口框；
  点击或拖拽即可跳转到画布任意位置，可收起，显隐偏好本地记忆
- **卡片「移动到…」** —— 卡片右键菜单新增二级菜单，可把卡片连同文件一起移动到
  指定分区（撤销时文件与卡片位置完整还原）
- **可折叠图标工具栏** —— 撤销 / 重做 / 新建便签等操作收进顶栏工具栏，
  一律纯图标（文字保留在悬停提示与无障碍标签），折叠状态本地记忆
- **分区选中** —— 分区可与卡片互斥选中，粘贴时以选中分区为确定性落点

### 变更

- **应用图标换成蓝橙无限符号图** —— 源图为 1080×1080 的 `src-tauri/icons/app-icon.jpg`，
  由 `pnpm icon` 派生全尺寸图标；不再使用脚本内绘制的 SDF 图形
- **图片卡片直接加载原图，不再生成缩略图** —— 空间文件夹内除原文件与隐藏的 `.mindscape\layout.json` 外不再产生任何文件（原每张图片会在 `.mindscape\thumbnails\` 生成一份无损 WebP 缩略图，照片类体积与原图同量级）；卡片宽高比改由只读图片头获得，视口内的卡片按需加载原图。已有旧缩略图缓存不再增长，可手动删除 `<空间文件夹>\.mindscape\thumbnails\` 目录回收空间
- **设置面板统一** —— 主题切换 / 显示已移除 / 检查更新收进同一个设置面板组件，
  空间列表页与画布页复用同一实现，两处入口与样式完全一致
- **画布页顶栏改为纯图标入口** —— 「新建空间」改为 ＋ 按钮，设置改为 ⚙ 图标，
  与其他图标按钮风格统一；按钮组钉在右侧，窄窗口下浮层不会跑出视口
- **分区名称字号调大** —— 提到 17px（显示态与改名输入框同步），折叠按钮同比放大
- **`Ctrl+V` 粘贴改为确定性落点** —— 由「按视口中心猜测位置」改为「选中分区 → 未分类」，
  粘贴位置可预期

### 修复

- **图片卡片缩放后不再比例失真、不再被裁掉** —— 拖动右下角手柄调整图片卡片大小时按原图的宽高比等比缩放（此前宽高各自独立变化，卡片比例会偏离原图）；同时修正图片元素的盒模型，卡片比原图更宽更扁时图片也不会被卡片边缘裁掉（此前 16:9 的图放进宽扁卡片会有一半看不到）

### 文档

- 新增 `HANDOVER.md`（交接文档，含数据 / 命令 / 渲染分层说明）与 `AGENTS.md`（协作约定）
- 新增 `docs/` 目录，收录缩略图重构（方案 A）的评估与实施记录
- README 同步方案 A：功能表、数据位置与已知限制

## [0.2.0] - 2026-09-12

### 新增

- **应用内检查更新** —— 集成 Tauri updater：启动时静默检查（只有真的发现新版本才提示），
  空间列表页右上角提供「检查更新」手动入口；安装包经 minisign 签名校验后下载安装，可一键重启生效
- 发版脚本 `pnpm release` —— 签名打包并生成自动更新所需的 `latest.json`
  （`pnpm release --manifest-only` 可用已有产物只重生成清单）

### 变更

- 应用标识由 `com.mindscape.app` 改为 `com.mindscape.canvas`
  —— 原值以 `.app` 结尾，与 macOS 应用包扩展名冲突，每次构建都会告警；改动不影响已存数据
- 工具链要求：pnpm 提升到 **≥ 10**（`pnpm-workspace.yaml` 使用 pnpm 10+ 的配置格式，pnpm 9 会解析失败）

### 文档

- README 补充「下载安装」与「自动更新」说明
- CONTRIBUTING 补充发版流程（签名密钥生成、发版步骤、Release 需上传的物料）

## [0.1.0] - 2026-09-12

首个预览版。功能范围对应开发计划书的 T0 ~ T3 全部工单。

### 新增

**准备层（T0）**

- 项目脚手架：Tauri v2 + React 18 + TypeScript 5（strict）+ Vite 5 + Tailwind 3 + Zustand 4
- 数据 Schema 与 zod 运行时校验（`src/core/types.ts`）
  - 读取落盘数据时校验，版本号高于当前 `DATA_VERSION` 时进入只读模式，一律不写盘
  - 布局文件损坏时不覆盖原文件，先备份 `.bak`，前端改用空布局并给出中文提示
- Rust 端文件命令：`list_dir` / `dir_exists` / `create_dir`、`read_layout` / `write_layout`
  - 落盘采用「500ms 防抖 + `.tmp` 原子重命名」
- 存储层接口 `StorageProvider` 与本地实现 `LocalFolderProvider`
- 命令系统与撤销重做栈（history 上限 50 步）
- 插件注册中心（预留 5 个扩展接口，第一版无插件管理界面）
- 菜单配置中心与卡片类型注册表（一份配置 + `appliesTo` 过滤）
- 交互底座：视口控制器与坐标换算；点击 / 拖拽 4px 判定工具

**阶段一（T1）**

- 空间列表页与新建空间；空间列表持久化，重启后自动恢复
- 进入画布：读取文件夹内容并网格铺开
- 缩略图生成与卡片渲染，保留原始宽高比
  - 缩略图为无损 WebP，缓存于 `<空间文件夹>\.mindscape\thumbnails\`
- 基础交互：滚轮缩放（10% ~ 400%，以鼠标位置为锚点）、画布平移、`Ctrl+0` 复位
- `layout.json` 读写打通，含视口 `zoom` / `offset` 保存
- 50 张图片加载性能自测通过

**阶段二（T2）**

- 卡片拖动；卡片缩放与多选
- 对齐辅助线与吸附
- 分区框：渲染、拖动整组、折叠、重命名、拖拽边缘自定义宽高
- 移除卡片（移入 `_已移除`，**不删除**）、已移除视图与恢复
- 撤销 / 重做，含被移除文件的回滚
- 大批量回滚性能保护
- 布局持久化

**阶段三（T3）**

- 卡片连线：从卡片边缘拖出箭头；连线标签与删除连线
- 卡片备注（`card.note` 持久化）
- 文字便签卡（`note` 类型）
- Rust 端 `open_with_default` / `reveal_in_explorer`
- 从资源管理器拖入文件（基于 Tauri `onDragDropEvent`）与落点规则
- 右键菜单浮层，由配置中心驱动动作登记
- `Ctrl+V` 粘贴剪贴板截图

**验收阶段补充**

- 深色模式与偏好记忆
- 复制 / 粘贴扩展到全部卡片类型（含跨分区）
- 移除卡片时级联断开其连线
- 界面文字整体放大，改善可读性

### 修复

- 修正滚轮缩放锚点漂移：坐标换算必须区分「被变换元素」与「作为参照元素」
- `image` crate 的 `thumbnail()` 会放大小图，改为先判断长边再决定是否缩放
- 统一 `-0` 与 `0`，避免缩放锚点出现 `-0` 偏移
- 修正 `-0` 归一、卡片资源路径不落盘等问题（详见各模块测试中的守卫用例）

### 已知限制

- 插件系统仅为占位，无管理界面
- 深色模式已完成配色变量与切换入口，未做全量视觉走查
- 缩略图采用无损 WebP，照片类缩略图体积约为 JPEG q80 的 2 ~ 4 倍
- 早期版本存在卡片 id 撞号，加载时会自动重编号修复；被重编号卡片上的原有连线端点可能失配（渲染层有兜底，不会崩溃）

[Unreleased]: https://github.com/weiyi251/Mindscape/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/weiyi251/Mindscape/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/weiyi251/Mindscape/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/weiyi251/Mindscape/releases/tag/v0.3.0
[0.2.0]: https://github.com/weiyi251/Mindscape/releases/tag/v0.2.0
[0.1.0]: https://github.com/weiyi251/Mindscape/releases/tag/v0.1.0
