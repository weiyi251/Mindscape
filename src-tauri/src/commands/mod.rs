// ============================================================================
// 模块说明（中文）
// Rust 端自定义命令模块入口。按职责分为五个子模块：
//   fs_ops    —— 目录读取 / 复制 / 移动 / 重命名
//   layout    —— .mindscape/layout.json 读写（原子写入 + 损坏容错）
//   thumbnail —— 缩略图生成
//   system    —— 调用系统默认程序打开文件
//   clipboard —— 系统剪贴板文件互通（2026-09-13，跨应用复制粘贴）
//
// 铁律（17.5）：所有命令返回 Result<T, String>，错误信息用中文；
// 接受绝对路径、不做隐式路径拼接；禁止在本层做业务判断。
//
// 实现任务：T0.5 / T0.6（fs_ops 与 layout 的部分命令）；其余在阶段一至三。
// ============================================================================

pub mod clipboard;
pub mod fs_ops;
pub mod layout;
pub mod system;
pub mod thumbnail;
