// ============================================================================
// 模块说明（中文）
// 插件目录入口。
//
// 第一版（v0.x）刻意只做占位：pluginCenter 提供 5 个接口，但**不暴露插件管理 UI**，
// 也不在任何生产路径上加载插件 —— 所以本文件当时只有一个空导出。
// （原注释写作「对应决策 31」，但仓库内没有任何文档锚点能检索到这个编号，
//   故改写成上面这句可读说明，避免后来者去追一个不存在的引用。）
//
// 第二版（插件功能）将在此统一导出内置插件清单，由 app/bootstrapPlugins.ts
// 在**首帧渲染前**遍历激活（注册必须早于第一次渲染，否则插件卡片会先被兜底样式渲染）。
// 完整设计见 docs/插件功能实施方案.md。
//
// 实现任务：插件功能（方案阶段，代码未开工）。
// ============================================================================

// Intentionally empty placeholder: 插件清单在插件功能开发时于此处补上。
export {}
