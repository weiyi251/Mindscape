// ============================================================================
// 模块说明（中文）
// 生命周期钩子注册入口。对应开发计划书第十三章「准备 2」的插件接口之一：
// 插件可响应「卡片被选中 / 被移动 / 被生成 / 被移除 / 分区框改名 / 空间打开 / 空间关闭」等事件。
//
// 实现任务：T0.9（准备层）—— 具体实现位于 core/registry/pluginCenter.ts，
// 本文件仅作为该目录的入口（barrel），供上层统一从 '@/core/hooks' 引入。
// ============================================================================

// TODO(T0.9): re-export hook registration APIs from the plugin center.
export {}
